import React from 'react';
import Styled from 'styled-components';

const Container = Styled.div`
    width: 100%;
`

export const SearchMovieSection: React.FunctionComponent = () => {
    return (
        <Container>

        </Container>
    )
}